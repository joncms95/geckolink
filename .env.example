# Copy to .env and set as needed. See docs/DEPLOY.md for production.

# ---- Local development (optional; defaults in config work for typical setup) ----
# DATABASE_URL=postgres://localhost:5432/geckolink_development
# REDIS_URL=redis://localhost:6379

# ---- Production / Docker (required for docker compose) ----
# Generate: openssl rand -hex 64
# SECRET_KEY_BASE=
# POSTGRES_PASSWORD=
