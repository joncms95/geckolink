# Database (optional in development; defaults from config/database.yml)
# DATABASE_URL=postgres://localhost:5432/w_coingecko_development

# Redis (optional in development; Sidekiq and optional redirect cache)
# REDIS_URL=redis://localhost:6379/0

# Production: set master key to decrypt credentials
# RAILS_MASTER_KEY=

# Production database password (if not using DATABASE_URL)
# W_COINGECKO_DATABASE_PASSWORD=
