export const DASHBOARD_PAGE_SIZE = 10
export const ERROR_DISMISS_MS = 5000
/** Duration for session-invalidated toast so the user can read it (ms). */
export const SESSION_INVALIDATED_TOAST_MS = 12_000
